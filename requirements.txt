# Python dependencies
# Install via
# pip install -r requirements.txt
jinja2
cmsis_svd
pyyaml
Pygments
jinja2_highlight
systemrdl-compiler
pytest

# DBML parser for database schema generation
pydbml

# IDL parser - modern alternative to EOL OpenSplice DDS
# Bridle: OMG IDL parser/AST library (actively maintained as of 2025)
git+https://github.com/iguessthislldo/bridle.git

